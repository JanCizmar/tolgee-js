name: Dummy1

on:
  push:

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
         touch aaa.txt
         git add .
         git commit -m "committed"
         git push
